- 采用扁平化目录结构，按业务领域（如 `blockchain`, `finance`, `mlops`）组织子模块。
- 每个技能单元以 `SKILL.md` 为核心入口，定义元数据、触发条件及工作流。
- 辅助资源通过标准化子目录管理：`scripts/` 存放执行脚本，`references/` 存放文档，`templates/` 存放模板。
- 依赖关系通过 `metadata.related_skills` 显式声明，支持跨技能协作。