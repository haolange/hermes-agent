- 所有测试文件必须遵循 `test_<module_name>.py` 命名规范，且位于对应源码模块的镜像测试目录下。
- 涉及外部 API 或敏感配置的测试必须通过 `monkeypatch` 或 `tmp_path` 模拟环境，严禁依赖本地真实凭证或网络资源。
- 异步测试统一使用 `pytest.mark.asyncio` 标记，并在需要时通过 `_ensure_current_event_loop` 兼容同步上下文调用。