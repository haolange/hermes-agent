- 所有模块通过 `hermes_constants.get_hermes_home()` 解析用户数据目录，支持 `HERMES_HOME` 环境变量覆盖以实现多实例隔离。
- 敏感信息（API Key、Token）在写入日志或环境前必须经过 `hermes_cli.config` 或 `gateway/run.py` 中的脱敏处理。
- CLI 与网关共享 `hermes_cli/commands.py` 中的命令注册表，确保 slash 命令在不同交互界面（终端、Telegram、Web）中定义一致。