- 所有涉及文件系统路径的操作（如任务 ID 目录、脚本路径）均进行严格的安全校验，禁止路径遍历或逃逸出指定沙箱目录。
- 任务状态更新（如标记运行、推进下次执行时间）必须在持有 `_jobs_file_lock` 锁的情况下执行，以防止竞态条件导致数据损坏。
- 采用“静默跳过”策略处理无输出或特定网关信号（如 `[SILENT]` 或 `wakeAgent: false`），避免产生无效的交付通知。