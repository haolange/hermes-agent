- 采用 YAML 配置文件（如 `web_research.yaml`）定义环境、工具集、模型参数及压缩策略。
- 使用 JSONL 格式（`example_browser_tasks.jsonl`）存储提示词数据集，供批量运行器读取。
- 提供 Shell 脚本（`run_browser_tasks.sh`）封装 `batch_runner.py` 调用，预设浏览器任务的分布比例与系统提示词。
- 包含独立的轨迹压缩配置（`trajectory_compression.yaml`），定义分词器、保护轮次及摘要生成逻辑，以优化训练数据长度。