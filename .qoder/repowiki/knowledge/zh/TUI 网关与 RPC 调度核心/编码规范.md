- 所有 RPC 处理方法均使用 `@method("name")` 装饰器进行注册，并统一接收 `(rid, params)` 参数，返回符合 JSON-RPC 规范的响应字典。
- 采用“延迟构建”策略，在 `session.create` 时仅初始化会话元数据，直到首次收到 `prompt.submit` 等实际请求时才在后台线程启动 `AIAgent` 和 `_SlashWorker`。
- 通过 `write_json` 统一输出响应，该函数会根据当前上下文变量或会话绑定的 `Transport` 自动路由输出流，屏蔽底层 stdio 与 WebSocket 的差异。