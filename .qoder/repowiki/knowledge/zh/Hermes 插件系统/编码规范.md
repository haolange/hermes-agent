- 每个插件目录必须包含 `plugin.yaml` 清单文件和 `__init__.py` 入口文件以支持自动发现。
- 平台适配器类统一继承自 `BasePlatformAdapter` 并实现 `connect`、`disconnect` 及消息处理生命周期方法。
- 模型提供商插件通过调用 `providers.register_provider()` 注册 `ProviderProfile` 实例来定义环境变量的映射与默认配置。
- 浏览器及外部服务提供者通过继承特定的 Provider 抽象类（如 `BrowserProvider`）并实现会话创建与销毁逻辑。