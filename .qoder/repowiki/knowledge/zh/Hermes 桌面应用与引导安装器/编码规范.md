- Electron 主进程逻辑使用 CommonJS (.cjs) 编写，而渲染层及共享库全面采用 ES Modules (.ts/.tsx)。
- 桌面端状态管理统一使用 nanostores 原子存储，并通过 @nanostores/react 的 useStore 钩子在组件中订阅。
- 后端通信严格遵循 JSON-RPC 2.0 规范，通过 shared 模块的 GatewayClient 发送请求并监听特定事件类型。
- 安装与引导流程采用分阶段状态机设计，通过 IPC 向渲染层广播 manifest、stage 进度及日志事件。