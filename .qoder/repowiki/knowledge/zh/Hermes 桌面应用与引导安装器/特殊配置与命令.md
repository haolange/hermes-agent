- **Desktop 开发**: `npm run dev` 并发启动 Vite 渲染进程与 Electron 主进程；`npm run dist:win/mac/linux` 执行 electron-builder 打包。
- **Bootstrap 开发**: `npm run tauri:dev` 启动 Tauri 开发环境；`npm run tauri:build` 编译并签名安装程序。
- **环境要求**: Node.js ^20.19.0 || >=22.12.0；Rust toolchain (针对 bootstrap-installer)。