进入 `website/` 后执行 `npm run start` 启动开发服务器（自动先跑 `scripts/prebuild.mjs` 生成技能/蓝图文档），`npm run build` 构建生产包，`npm run build:fast` 仅构建英文 locale，`npm run write-translations` 可从已渲染页面提取翻译占位符，`npm run typecheck` 执行 TypeScript 类型检查。