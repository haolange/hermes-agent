Python + `websockets` 实现 CDP WebSocket 客户端；通过子进程调用 Node.js `agent-browser` CLI 驱动 Playwright/Chromium；可选 Camofox（Node.js REST API，基于 Camoufox Firefox 指纹伪装）；通过 `plugins/browser/<vendor>/` 插件注册表接入 Browserbase、Browser Use、Firecrawl 等云后端。