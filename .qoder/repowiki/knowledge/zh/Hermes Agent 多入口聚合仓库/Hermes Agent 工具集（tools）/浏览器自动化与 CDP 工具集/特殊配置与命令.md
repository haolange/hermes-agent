本地模式需先执行 `npx agent-browser install`（或 `--with-deps` 安装系统库）；Termux 环境要求显式 `npm install -g agent-browser && agent-browser install`；Camofox 需单独启动 `camofox-browser` 服务并将 `CAMOFOX_URL` 写入 `.env`；CDP 模式需通过 `/browser connect` 设置 `BROWSER_CDP_URL` 或在 `config.yaml` 中配置 `browser.cdp_url`。