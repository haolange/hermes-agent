NeuTTS 需通过 `python -m tools.neutts_synth --text ... --out ... --ref-audio ... --ref-text ...` 子进程调用；sherpa-onnx 唤醒词模型首次使用时自动从 GitHub Releases 下载约 13MB tar.bz2 并解压至 `HERMES_HOME/cache/wakewords`；macOS ARM64 上 openWakeWord 默认强制使用 tflite 后端并需要 `pip install ai-edge-litert` 桥接 `tflite_runtime.interpreter`。