异步委派完成后通过 `process_registry.completion_queue` 以 `type='async_delegation'` 事件重新进入 CLI/gateway 主循环，无需新增消费者；`recover_abandoned_delegations` 在启动时扫描 `owner_pid` 已消失的行标记为 `unknown`，`restore_undelivered_completions` 将 pending 完成事件重放并打上 `restored=True` 标记。