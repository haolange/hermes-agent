SQLite（`state.db` 持久化异步委派状态，含 WAL 迁移）、`concurrent.futures.ThreadPoolExecutor` + `tools.daemon_pool.DaemonThreadPoolExecutor`（守护线程池）、`jsonschema`（可选，用于输出契约校验）、`httpx`（异步媒体上传预签名 PUT）、`urllib.parse`（SSRF 安全的 URL 构造）。