基于 Python `mcp` SDK 的 `ClientSession`、`streamablehttp_client`、`sse_client` 与 `OAuthClientProvider`；HTTP 回调使用标准库 `http.server.BaseHTTPRequestHandler`；令牌持久化采用原子 JSON 写入（`os.open(O_EXCL)` + `os.replace`）；POSIX 下通过 `killpg` + `start_new_session` 实现子进程组清理。