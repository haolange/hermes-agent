stdio MCP 子进程在 POSIX 上必须经 `python3 -m tools.mcp_stdio_watchdog --ppid <pid> -- <command> ...` 包装；OAuth 首次授权需交互式完成 `hermes mcp login <server>`，非交互环境会抛出 `OAuthNonInteractiveError`；令牌目录位于 `HERMES_HOME/mcp-tokens/`，schema 缓存位于 `HERMES_HOME/cache/mcp_schema_cache.json`。