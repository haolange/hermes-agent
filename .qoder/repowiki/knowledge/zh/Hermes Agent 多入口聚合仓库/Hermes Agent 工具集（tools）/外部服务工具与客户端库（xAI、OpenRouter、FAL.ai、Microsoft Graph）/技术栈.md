requests + httpx 作为底层 HTTP 客户端；OpenAI Python SDK（通过 `agent.auxiliary_client` 路由至 OpenRouter）；FAL.ai SDK (`fal_client`) 经 `lazy_deps` 懒加载；Microsoft Graph 使用 Azure AD v2.0 `client_credentials` 授权；xAI 支持 SuperGrok OAuth 与 `XAI_API_KEY` 两种凭据路径。