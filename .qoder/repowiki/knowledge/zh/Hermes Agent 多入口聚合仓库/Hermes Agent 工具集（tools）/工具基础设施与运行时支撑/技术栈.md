Python 标准库（subprocess、threading、contextvars、concurrent.futures、ast、difflib）+ git CLI（通过 `GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE` 使用 bare repo 做快照）+ psutil（进程树枚举/终止）+ systemd-run/systemctl（可选的 cgroup 隔离）+ PyYAML（蓝图 frontmatter 解析）。