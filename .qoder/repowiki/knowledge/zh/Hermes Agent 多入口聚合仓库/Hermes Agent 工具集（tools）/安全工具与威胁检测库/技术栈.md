Python 标准库（`pathlib`、`re`、`ipaddress`、`subprocess`、`tarfile`、`urllib.request`、`threading`）；外部二进制 tirith（sheeki03/tirith，GitHub Release 自动安装，可选 cosign 供应链验证）；Google OSV API（api.osv.dev）用于 MCP 扩展恶意包检测；httpx/httpcore 传输层插桩实现连接时 SSRF 校验。