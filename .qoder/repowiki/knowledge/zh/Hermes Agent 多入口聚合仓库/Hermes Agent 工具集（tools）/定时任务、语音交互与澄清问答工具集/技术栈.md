Python 标准库 + 可选第三方库：`sounddevice`/`numpy`（音频采集与合成）、`wave`（WAV 编码）、`subprocess`（Termux:API 麦克风命令）、`threading`（事件/锁/线程）；macOS 下通过 `afplay` 绕过 PortAudio TCC 权限提示；Linux 通过 PulseAudio/PipeWire socket 探测实现 SSH/容器音频转发。