语音模式需额外安装可选依赖：`pip install sounddevice numpy` 或 `uv sync --extra voice`；Termux 上需安装 `termux-api` 包及 Android 端 Termux:API 应用并使用 `pkg install portaudio` 提供系统库；Windows WSL 下需设置 `PULSE_SERVER=unix:/mnt/wslg/PulseServer` 并配置 ALSA 指向 PulseAudio 才能录音。