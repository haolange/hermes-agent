通过环境变量选择执行后端：`TERMINAL_ENV`（local/docker/modal/vercel_sandbox）；超时上限 `TERMINAL_MAX_FOREGROUND_TIMEOUT`；磁盘告警阈值 `TERMINAL_DISK_WARNING_GB`；Vercel 需设置 `VERCEL_TOKEN`/`VERCEL_PROJECT_ID`/`VERCEL_TEAM_ID` 或 `VERCEL_OIDC_TOKEN`；sudo 交互密码可通过 `SUDO_PASSWORD` 配置或由 CLI 注册的回调提供。