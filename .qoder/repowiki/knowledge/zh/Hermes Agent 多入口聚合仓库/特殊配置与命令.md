顶层提供 `python cli.py` 交互式 CLI、`python run_agent.py` 直接运行 agent、`python mcp_serve.py` 启动 MCP 服务、`python batch_runner.py` 批量执行；容器化通过 `Dockerfile` 与 `docker-compose.yml` 一键拉起 gateway+web；Nix 开发环境由 `flake.nix` 管理。