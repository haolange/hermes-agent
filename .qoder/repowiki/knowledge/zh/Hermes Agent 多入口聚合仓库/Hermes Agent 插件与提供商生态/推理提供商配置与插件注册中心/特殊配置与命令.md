新增提供商无需修改本模块，只需在 `plugins/model-providers/<name>/__init__.py` 中实例化 `ProviderProfile` 并调用 `register_provider()`，或在 `$HERMES_HOME/plugins/model-providers/` 下放置同名目录以覆盖内置 profile；首次访问 registry 时自动发现，无需显式构建或安装步骤。