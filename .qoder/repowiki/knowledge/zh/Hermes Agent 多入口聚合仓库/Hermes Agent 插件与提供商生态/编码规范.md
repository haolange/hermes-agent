- 每个插件子目录必须包含 plugin.yaml 描述元数据（name、env_vars、base_url 等）以及 __init__.py 导出 ProviderProfile 或工具函数
- 外部客户端通过 plugin_utils.lazy_singleton 或 SingletonSlot 包装，避免并发初始化导致的资源泄漏
- ProviderProfile 采用声明式字段而非构造参数传递行为差异，具体请求/消息转换通过 get_max_tokens/build_extra_body/fetch_models 等钩子方法扩展