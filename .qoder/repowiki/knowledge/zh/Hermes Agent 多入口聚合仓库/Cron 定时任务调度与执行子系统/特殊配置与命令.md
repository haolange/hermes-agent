Gateway 通过 `hermes gateway install` / `sudo hermes gateway install --system` 安装为系统服务后自动启动 ticker；CLI 命令 `hermes cron create/list/pause/resume/remove/status` 操作 jobs.json；`hermes cron status` 读取 `ticker_heartbeat` / `ticker_last_success` 判断 ticker 存活与最近失败原因；支持 `cron.provider` 配置切换外部调度器。