Python 标准库 + asyncio/starlette WebSocket；JSON-RPC 2.0 over newline-delimited JSON；通过 `concurrent.futures.ThreadPoolExecutor` 调度慢 RPC；子进程 via `subprocess.Popen` 运行独立 `slash_worker`；MCP 工具发现委托给 `hermes_cli.mcp_startup`。