Python ≥3.11,<3.14（pyproject requires-python），依赖通过 uv.lock 精确锁定；Windows 平台通过 hermes_bootstrap 强制 UTF-8 编码；可选功能按 extras（messaging/mcp/web/voice/wake 等）与 tools/lazy_deps.py 延迟安装；SQLite + FTS5 CJK 分词扩展（native/）用于会话全文检索。