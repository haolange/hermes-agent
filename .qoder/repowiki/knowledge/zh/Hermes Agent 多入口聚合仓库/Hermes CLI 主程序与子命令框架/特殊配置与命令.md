`hermes --version` 走 `_startup_fast.try_fast_version()` 超快路径，跳过所有第三方导入；`hermes -p/--profile` 在 argparse 之前被手动预解析并改写 `HERMES_HOME`；Windows 下通过 `hermes_bootstrap` 和 `_subprocess_compat.suppress_platform_ver_console()` 避免控制台闪烁；Termux 环境会触发 bundled skills 同步与更新预取逻辑。