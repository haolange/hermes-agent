Electron 40 + Vite 8 + React 19 + TypeScript 6（桌面端）；Tauri 2 + Rust 1.77 + tokio + reqwest（引导安装器）；构建使用 electron-builder（mac dmg/zip、win nsis/msi、linux AppImage/deb/rpm）与 tauri-cli；测试采用 Vitest（单元）+ Playwright（e2e，含 headless cage 视觉快照）。