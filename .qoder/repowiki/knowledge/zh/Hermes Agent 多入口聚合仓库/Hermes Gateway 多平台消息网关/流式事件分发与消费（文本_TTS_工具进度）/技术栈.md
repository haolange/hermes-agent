Python asyncio + threading + queue.Queue 实现 sync→async 桥接；使用 frozen dataclass 作为跨线程安全的事件载体；Telegram 原生 draft streaming（`sendDraft`）与通用 edit-in-place（`editMessageText`）双通道；TTS 路径通过 `tools.tts_streaming.SentenceChunker` 与 `StreamingTTSProvider` 抽象不同语音后端。