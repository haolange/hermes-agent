- 每个平台以独立子包形式置于 platforms/<platform>/ 并通过 platforms.base.PlatformAdapter 抽象接入，新增平台遵循 ADDING_A_PLATFORM.md 约定。
- 用户可见文本在离开网关前统一经过 _redact_gateway_user_facing_secrets / _sanitize_gateway_final_response 进行敏感信息脱敏与 provider 错误归一化。
- 可观测性通过 hooks.py 钩子系统与 status/status_phrases.yaml 集中声明，而非在各平台内散写日志。
- 运行时开关（如 compression.progress_notices、auto_continue_freshness）一律从 config.yaml 读取并经环境变量 HERMES_* 覆盖，保持 fail-closed 默认。