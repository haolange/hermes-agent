Python `contextvars.ContextVar` 实现任务本地会话上下文；`asyncio.Lock` + 自定义 `TurnLeaseToken` 实现跨路由键的 per-session_id 串行化；dataclass + MutableMapping 适配器模式做 legacy 兼容；通过 `HERMES_AUTO_CONTINUE_FRESHNESS` 环境变量配置自动续期窗口。