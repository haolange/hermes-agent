无专用构建脚本；模块通过 `hermes_cli.config.get_hermes_home()` 定位 `~/.hermes` 数据目录，hook 需放在该目录下 `hooks/<name>/HOOK.yaml` + `handler.py`，配对数据存放在 `{HERMES_HOME}/platforms/pairing/`（兼容旧布局 `{HERMES_HOME}/pairing/` 并自动合并），PID/锁/状态文件位于 `{HERMES_HOME}/gateway.pid`、`gateway.lock`、`gateway_state.json`。