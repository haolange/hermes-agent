可通过 `python -m acp_adapter`、`python -m acp_adapter.entry` 或 `hermes acp` 启动；支持 `--version`、`--check`（验证依赖）、`--setup`（交互式模型/provider 配置）、`--setup-browser --yes`（安装 agent-browser + Playwright Chromium）。首次启动时可选后台线程执行 MCP 工具发现（可通过 `HERMES_ACP_SKIP_CONFIGURED_MCP=1` 禁用）。