`web`：`npm run dev`（Vite 开发服务器）、`npm run build`（tsc -b && vite build）、`npm run test`（vitest run）；`ui-tui`：`npm run start`（tsx 直接运行 entry）、`npm run dev`（tsx --watch 并先构建 hermes-ink）、`npm run build`（调用 scripts/build.mjs 打包），视觉回归脚本位于 `scripts/visual/run.mjs`。