运行方式：Python 测试通过仓库根 `scripts/run_tests.sh` 或 `scripts/run_tests_parallel.py` 启动（单进程/多进程），JS 测试通过 `cd tests-js && npm test`（等价于 `vitest run`）。测试运行前会自动将 `HERMES_HOME` 重定向到临时目录并清空所有凭据类环境变量，开发者无需额外准备即可运行。