- 测试文件以 `test_*.py` / `*.test.ts` 命名并按被测子系统分目录存放，与源码目录结构一一对应。
- 通过 `conftest.py` 集中声明夹具，并在模块顶层（fixture 之外）完成进程级沙箱初始化（重置 `HERMES_HOME`、清理凭据环境变量、固定时区与哈希种子）。
- 凭据相关环境变量通过白名单后缀（`_API_KEY`、`_TOKEN`、`_SECRET`、`_PASSWORD` 等）和显式名称列表统一过滤，避免本地密钥泄漏到断言中。
- 测试按“每文件一进程”粒度运行，文件内测试顺序由作者负责保证无状态依赖，不依赖 fixture 做跨测试状态清理。
- JS 测试统一放在 `tests-js/` 并使用 Vitest 的 `node` 环境，配置文件集中在 `vitest.config.ts`，类型检查与 lint 通过 `npm run check` 串联。