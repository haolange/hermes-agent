Python 标准库 `concurrent.futures.ThreadPoolExecutor` + `types.SimpleNamespace` 用于 MoA 并发编排与轻量响应对象；SSE 原始事件流消费（`response.completed`/`output_item.done`/`error` 帧）；通过 `agent.usage_pricing.CanonicalUsage` 统一 token 用量归一化与成本估算。