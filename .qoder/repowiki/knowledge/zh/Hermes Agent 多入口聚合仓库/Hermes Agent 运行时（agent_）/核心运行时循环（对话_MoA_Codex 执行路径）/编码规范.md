- 所有对外函数均以 `agent` 作为第一个参数，通过 `getattr(agent, ...)` 安全读取可选回调/状态，避免强耦合。
- 对第三方/可选依赖采用函数内 `import` 延迟加载（如 `agent.transports.codex_app_server_session`、`run_agent._StreamErrorEvent`、`hermes_cli.*`），保证模块可被无副作用地导入。
- 外部 I/O 与 UI 回调一律用 try/except 包裹并以 `logger.debug` 记录异常，确保单点失败不会中断主循环或事件流。
- token 用量与成本统计统一经由 `CanonicalUsage` + `estimate_usage_cost` 归一化后再写入 `agent.session_*` 计数与 `_session_db.queue_token_counts`。
- MoA 中通过线程级缓存（带 TTL 与锁）复用已解析的 runtime/provider 配置与 context-length 查询，避免每轮重复 I/O。
- 面向用户的中间态输出（tool progress、stream delta、interim assistant message）通过代理回调分发，并在桥接层做去重与预览裁剪，保持 TUI/gateway 一致体验。