Python 标准库 `ssl`（`create_default_context`/`get_ca_certs`）、`pathlib.Path` 进行路径规范化；依赖 `certifi` 包提供的系统 CA bundle；通过环境变量 `HERMES_CA_BUNDLE`、`SSL_CERT_FILE`、`REQUESTS_CA_BUNDLE`、`CURL_CA_BUNDLE` 与第三方 HTTP 客户端（httpx/OpenAI）对齐 CA 配置。