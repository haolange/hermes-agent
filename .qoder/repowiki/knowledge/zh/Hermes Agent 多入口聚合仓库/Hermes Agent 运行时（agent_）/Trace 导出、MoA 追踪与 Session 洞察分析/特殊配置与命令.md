上传 trace 需设置 `HF_TOKEN`/`HUGGINGFACE_HUB_TOKEN`/`HUGGING_FACE_HUB_TOKEN`/`HUGGINGFACE_TOKEN` 环境变量之一，且 token 需具备 WRITE 权限；MoA 追踪默认关闭，需在配置中启用 `moa.save_traces`，可通过 `moa.trace_dir` 覆盖默认 `<hermes_home>/moa-traces/` 目录。