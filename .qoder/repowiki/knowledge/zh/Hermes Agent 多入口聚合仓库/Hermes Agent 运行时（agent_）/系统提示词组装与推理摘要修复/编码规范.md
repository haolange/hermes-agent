- 所有提示词片段以模块级常量或纯函数（如 `computer_use_guidance(platform_name)`）形式暴露，调用方只消费返回值，不持有状态。
- 提示词按 stable/context/volatile 三层组织，volatile 层放在末尾以保证最长前缀缓存命中。
- 对外部运行时对象（如 run_agent、config、memory_store）采用延迟导入或属性访问，避免模块加载期副作用并保留测试 patch 能力。
- 平台/模型相关分支通过字符串匹配（如 `"gemini" in model_lower`、`sys.platform == "darwin"`）选择注入的提示段，新增模型或平台只需扩展匹配列表。
- 上下文文件注入前先经 `_scan_context_content` 调用 `scan_for_threats(scope="context")`，命中则替换为 `[BLOCKED: ...]` 占位而非透传原文。
- 可配置行为统一通过 `getattr(agent, "_xxx", default)` 读取 agent 实例上的开关（如 `_task_completion_guidance`、`_parallel_tool_call_guidance`、`_environment_probe`），默认值保持开启。