新增一个凭证来源需要三步：在 `_seed_from_*` 中添加读取分支、用 `match_fn` 接入 `is_source_suppressed` 抑制逻辑、在 `credential_sources._register_all_sources()` 中 `register(RemovalStep(...))`；注册顺序重要，provider-specific 步骤必须放在通用 `env:*` 之前。