引用展开依赖外部命令：`git`（diff/log）、可选的 `rg`（快速目录扫描，回退到 `os.walk`）；Windows 下通过 `windows_hide_flags()` 隐藏终端弹窗。`,/context` 视图由 `render_context_grid`/`render_context_category_lines`/`render_context_details_lines` 生成，CLI 传 `grid=True`，gateway 传 `grid=False`。