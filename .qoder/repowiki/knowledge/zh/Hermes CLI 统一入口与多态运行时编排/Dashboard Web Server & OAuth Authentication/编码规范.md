- 在 `web_server.py` 和 `dashboard_auth/middleware.py` 中均采用分层中间件模式，依次执行 Host 头防重绑定检查、OAuth 会话网关校验及遗留会话令牌鉴权。
- 在 `dashboard_register.py` 和 `webhook.py` 中对本地敏感配置文件（`.env` 或 `webhook_subscriptions.json`）的写入均通过临时文件原子替换实现，并强制设置 0o600 权限。
- 认证相关模块（`dashboard_auth/base.py` 和 `registry.py`）广泛使用数据类（dataclasses）定义不可变会话对象，并通过线程锁保护全局提供者注册表的状态一致性。