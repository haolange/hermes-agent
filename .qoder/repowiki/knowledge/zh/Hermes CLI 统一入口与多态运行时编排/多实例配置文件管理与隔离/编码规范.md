- 所有配置文件名称在存入磁盘或用于CLI参数前，必须经过`normalize_profile_name`进行标准化处理（转为小写，特殊处理'default'）。
- 涉及敏感信息的文件（如`.env`）在克隆或复制后，必须显式调用`os.chmod`将权限收紧为所有者只读（0o600）。
- 在执行可能影响系统状态的操作（如删除配置文件、停止网关）前，必须先检查并优雅地停止正在运行的网关进程，防止资源泄漏。
- 导入外部归档文件时，必须通过`_normalize_profile_archive_parts`校验路径成员，严禁绝对路径和`..`遍历，确保解压安全。