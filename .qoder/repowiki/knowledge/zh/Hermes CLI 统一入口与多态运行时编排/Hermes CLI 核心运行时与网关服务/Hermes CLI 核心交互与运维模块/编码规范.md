- 配置优先采用 `~/.hermes/config.yaml` 作为权威数据源，`.env` 仅用于敏感凭证，且在代码中明确处理两者冲突时的优先级（通常 config 覆盖 env）。
- 所有涉及用户输入的敏感信息（API Keys、Tokens）均通过 `masked_secret_prompt` 或密码模式读取，并禁止在日志或标准输出中明文打印。
- 模块初始化阶段广泛采用“尽力而为”（best-effort）的异常捕获策略，确保单个组件（如日志、网络检测）的失败不会导致整个 CLI 启动崩溃。