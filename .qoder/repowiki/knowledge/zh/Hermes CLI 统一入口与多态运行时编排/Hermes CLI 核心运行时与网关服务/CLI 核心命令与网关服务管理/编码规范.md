- 在 CLI 子命令中使用局部导入（local imports）来引用重型依赖（如 `AIAgent`、`send_message_tool`），以保持顶层 CLI 的快速响应。
- 在服务管理操作中采用“先检查后执行”的模式，例如在安装 systemd 单元前检查是否存在冲突的旧版单元或权限问题。
- 通过环境变量（如 `HERMES_YOLO_MODE`、`HERMES_INFERENCE_MODEL`）实现非交互式模式下的行为覆盖和配置回退。