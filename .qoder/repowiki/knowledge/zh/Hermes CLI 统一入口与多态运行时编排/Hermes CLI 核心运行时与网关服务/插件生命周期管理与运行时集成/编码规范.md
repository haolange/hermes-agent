- 插件必须包含 `plugin.yaml` 清单文件以及带有 `register(ctx)` 入口函数的 `__init__.py` 模块。
- 所有插件注册的回调函数（Hooks/Middleware）均被独立的 try/except 块包裹，确保单个插件异常不会中断核心代理循环。
- 采用路径派生键（如 `image_gen/openai`）作为插件唯一标识，防止不同类别下同名插件（如 `tts/openai`）发生命名冲突。