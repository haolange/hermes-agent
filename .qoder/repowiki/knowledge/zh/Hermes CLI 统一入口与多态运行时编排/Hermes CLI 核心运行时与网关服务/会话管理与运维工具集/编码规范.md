- CLI 命令处理函数通常采用 `_cmd_<action>` 或 `run_<feature>` 命名模式，并接受 `args` 对象作为唯一参数以保持一致性。
- 在执行破坏性或迁移操作前，普遍采用“预览-确认-执行”的三阶段模式，并提供 `--dry-run` 和 `--yes` 标志以支持自动化与非交互式场景。
- 涉及文件状态变更的操作（如备份、迁移）均内置自动回滚或快照机制，并在操作失败时提供明确的恢复指令（如 `hermes import <backup_file>`）。