- 所有外部HTTP请求均使用标准库`urllib`或`httpx`并包裹在宽泛的try/except中，确保探测失败时返回降级结果而非抛出异常。
- 账户状态数据统一封装为`@dataclass(frozen=True)`不可变对象（如`NousPortalAccountInfo`），以保障多线程环境下的读取安全。
- 配置项检查遵循“显式覆盖优先”原则，即在自动注入默认值前，先校验环境变量或配置文件中的显式设置（如`use_gateway`标志）。