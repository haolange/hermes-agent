- **全局前置拦截与上下文注入**：在 `main.py` 模块加载初期执行 Profile 解析（`_apply_profile_override`）、环境变量加载（`.env`）及安全策略同步（如 `HERMES_REDACT_SECRETS`），确保所有子模块在统一的隔离上下文中运行。
- **跨平台服务生命周期管理**：通过 `gateway.py` 抽象 systemd/launchd/Windows 服务差异，提供统一的 `hermes gateway` 控制接口，并集成进程表扫描与 PID 文件校验以处理僵尸进程。
- **动态路由与协议适配**：内置轻量级 HTTP 代理服务器（`proxy/server.py`）和 Dashboard OAuth 注册流程（`dashboard_register.py`），实现从本地 CLI 到远程 AI 提供商或 Web UI 的无缝凭证桥接与流量转发。