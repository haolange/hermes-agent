- 在模块导入前通过预解析 `sys.argv` 处理 `--profile` 标志并设置 `HERMES_HOME` 环境变量，确保后续配置加载的正确性。
- 采用“最佳努力”（best-effort）的初始化策略，对日志设置、UTF-8 编码强制等关键但非致命的操作使用 try-except 包裹，防止因环境异常导致 CLI 崩溃。
- 将命令定义集中在 `COMMAND_REGISTRY` 数据类中，通过统一的注册表驱动 CLI 帮助信息、网关分发逻辑以及外部平台的命令映射。