- 所有敏感或非敏感的环境变量在安装时均统一通过 `save_env_value` 存入 `~/.hermes/.env`，遵循单一凭证存储原则。
- Git 安装采用确定性策略，每次安装前若目标目录存在则先执行 `shutil.rmtree` 清除，再重新克隆指定 ref。
- 工具选择逻辑遵循优先级回退机制：优先保留用户历史选择，其次使用 manifest 定义的 `default_enabled`，最后默认全选。
- 在非 TTY 环境下自动跳过交互式 checklist，直接根据预设优先级应用工具过滤规则，确保自动化脚本兼容性。