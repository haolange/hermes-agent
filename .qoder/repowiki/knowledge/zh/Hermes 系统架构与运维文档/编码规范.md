- 中间件与钩子注册均通过 `register(ctx)` 入口统一声明，遵循 fail-open（失败开放）原则以确保系统稳定性。
- 可观测性载荷强制使用稳定的关联 ID（如 `session_id`, `turn_id`, `api_request_id`）以实现跨事件追踪，且禁止解析不透明的 ID 格式。
- 安全隔离采用“内部网络 + 出口代理”的双层 Docker 网络架构，默认阻断容器出站流量并仅允许白名单域名。