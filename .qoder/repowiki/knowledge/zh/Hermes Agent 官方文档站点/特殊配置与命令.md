- **预构建流程**：执行 `npm run start` 或 `npm run build` 时会自动运行 `scripts/prebuild.mjs`，该脚本负责拉取远程技能索引、生成本地技能页面及 LLM 专用文档摘要。
- **LLM 友好接口**：自动生成 `/docs/llms.txt`（精简索引）和 `/docs/llms-full.txt`（全量文档拼接），专为 AI 代理上下文摄入优化。