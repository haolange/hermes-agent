- 所有自动生成的技能文档页首均包含特定注释 `{/* This page is auto-generated ... */}`，明确禁止人工直接编辑，确保内容与 `skills/` 目录下的源文件保持同步。
- 文档搜索配置中通过 `ignoreFiles` 正则表达式显式排除 `user-guide/skills/bundled/` 和 `optional/` 下的海量自动生成页面，以防止通用术语搜索结果被技能详情页淹没。
- 在生成 Markdown 内容时，对包含 Unicode 框线字符的代码块自动包裹 `<!-- ascii-guard-ignore -->` 标记，以绕过站点的 ASCII 艺术检查 lint 规则。