- 采用扁平化 YAML 文件结构，每个文件代表一种语言区域（如 `en.yaml`, `zh.yaml`）。
- 以 `en.yaml` 为基准（Source of Truth），定义了统一的点号分隔键路径结构（如 `approval.dangerous_header`）。
- 所有语言文件必须保持键集合的严格同步，通过 `{placeholder}` 语法支持动态内容注入。
- 模块仅包含纯数据文件，无逻辑代码，依赖外部 i18n 加载器进行运行时解析。