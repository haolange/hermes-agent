- 所有涉及磁盘写入的操作（如 JSON 缓存、会话索引）均使用原子写入模式（先写临时文件再重命名），以防止数据损坏。
- 流式消费者中的文本过滤采用状态机机制，精确识别并丢弃模型输出的 `<think>` 等推理标签块，确保用户界面整洁。
- 平台适配器的注册与发现通过 `platform_registry.py` 统一管理，支持插件动态注册并回退至内置适配器，避免硬编码的条件分支。
- 显示配置解析遵循“平台特定覆盖 > 全局用户设置 > 平台默认值 > 全局默认值”的四层优先级链，确保不同平台（如 Telegram vs Slack）的最佳展示效果。