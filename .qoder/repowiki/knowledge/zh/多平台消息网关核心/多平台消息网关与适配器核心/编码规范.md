- 采用 `_coerce_*` 系列工具函数（如 `_coerce_bool`, `_coerce_float`）统一处理配置值的类型转换与默认值回退，确保配置加载的健壮性。
- 在 `BasePlatformAdapter` 中定义统一的 `SendResult` 和 `MessageEvent` 数据模型，屏蔽不同平台（如 Telegram 与 Discord）在消息结构和发送反馈上的差异。
- 使用 `validate_media_delivery_path` 等安全校验函数，结合白名单目录与文件新鲜度检查，防止模型生成的路径导致敏感文件泄露或 SSRF 攻击。