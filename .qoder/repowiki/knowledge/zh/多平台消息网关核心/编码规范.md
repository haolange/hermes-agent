- 所有平台适配器必须继承 `BasePlatformAdapter` 并实现 `connect`、`disconnect`、`send` 等核心接口。
- 流式消费采用“同步回调入队 + 异步任务消费”模型，确保 Agent 工作线程与平台 I/O 线程的安全隔离。
- 媒体文件在投递前统一缓存至 `HERMES_HOME/cache/` 下的分类目录（images/audio/videos），以应对平台临时链接过期问题。