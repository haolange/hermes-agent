- **开发环境代理**：`vite.config.ts` 中配置了自定义插件 `hermesDevToken`，在开发模式下从后端 `hermes dashboard` 服务抓取并注入一次性 Session Token，解决本地开发时的 401 鉴权问题。
- **启动命令**：通过 `npm run dev` 启动 Vite 开发服务器，默认代理 `/api` 和 `/dashboard-plugins` 请求至 `http://127.0.0.1:9119`（可通过 `HERMES_DASHBOARD_URL` 环境变量修改）。