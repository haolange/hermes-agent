- 安装脚本采用“阶段协议”（Stage Protocol）设计，支持通过 `-Manifest` 和 `-Stage` 参数将安装过程拆解为可被 GUI 或 CI 调度的独立原子步骤。
- 跨平台安装逻辑遵循“托管优先”原则，优先在 `$HERMES_HOME` 目录下安装和管理 uv、Node.js 等工具链，以避免污染系统全局环境并解决权限问题。
- 桥接服务实施严格的 Host 头校验与用户白名单机制（Allowlist），仅接受本地回环请求并过滤非授权用户的消息输入，以防止 DNS 重绑定攻击和未授权访问。