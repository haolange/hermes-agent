- **FAL.ai SDK**：用于高性能图像与视频生成，支持 FLUX, Recraft 等多模型切换（见 `image_generation_tool.py`）。
- **Microsoft Graph API**：通过 `microsoft_graph_auth.py` 实现 OAuth2 客户端凭证流认证，访问 Office 365 资源。
- **SoundDevice & NumPy**：用于 `voice_mode.py` 中的实时音频流捕获、RMS 静音检测及 WAV 编码。