- 所有工具处理函数必须返回 JSON 格式的字符串，错误情况统一使用 `tool_error()` 或 `json.dumps({"error": ...})` 包装。
- 涉及外部 API 调用的工具需实现 `check_..._requirements()` 或类似的检查函数，并在注册时传入 `check_fn` 以控制工具在运行时的可见性。
- 对于可能阻塞或耗时的 I/O 操作（如网络请求、音频录制），优先使用异步处理或通过 `_run_async` 桥接，确保主线程响应性。