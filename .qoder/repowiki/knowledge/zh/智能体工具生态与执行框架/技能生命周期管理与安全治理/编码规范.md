- 所有对技能文件的修改（创建、编辑、补丁）均采用“临时文件写入 + `os.replace`”的原子化操作模式，确保在进程崩溃时不会留下损坏的文件。
- 在执行任何技能内容变更后，若配置启用，会自动触发 `skills_guard` 进行安全扫描；若扫描结果为“危险”，则立即回滚之前的文件变更并删除新创建的目录。
- 技能元数据（如名称、描述、平台限制）统一从 `SKILL.md` 的 YAML frontmatter 中解析，解析逻辑优先使用 `CSafeLoader` 并包含降级到简单键值对处理的容错机制。