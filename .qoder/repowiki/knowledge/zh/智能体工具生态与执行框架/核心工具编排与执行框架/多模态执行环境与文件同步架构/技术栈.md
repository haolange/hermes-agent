- **云端沙箱 SDK**：集成 `daytona` (Python SDK) 和 `modal` (Async Sandbox API) 进行资源编排。
- **容器运行时**：支持 `docker` CLI 交互及 `apptainer/singularity` 实例化管理。
- **远程协议**：基于 OpenSSH (`ssh`/`scp`) 及 ControlMaster 连接复用技术。