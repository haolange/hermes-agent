- 所有工具处理器必须返回 JSON 字符串格式的结果，错误统一通过 `tool_error` 辅助函数构造。
- 工具可用性检查函数 (`check_fn`) 的结果默认启用 30 秒 TTL 缓存，以减少对外部状态（如 Docker 守护进程）的频繁探测。
- 涉及大内容传输的操作（如文件写入、工具结果持久化）优先通过 stdin 管道传递数据，避免命令行参数长度限制。
- 工具模块通过在顶层调用 `registry.register()` 实现自注册，注册逻辑与业务逻辑解耦。