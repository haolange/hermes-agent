- 所有工具模块通过调用 `registry.register()` 在模块加载时自动向中央注册表声明其 Schema 和处理函数。
- 技能文件内容扫描仅针对 `SCANNABLE_EXTENSIONS` 定义的文本扩展名，跳过二进制文件以避免解码错误。
- 信任等级解析支持源标识符的前缀别名映射（如 `skills-sh/` 映射到具体仓库），并严格区分 `builtin` 和 `trusted` 来源。
- MCP 服务器的 stdio 子进程 stderr 被重定向到共享日志文件 `~/.hermes/logs/mcp-stderr.log`，防止污染 TUI 显示。